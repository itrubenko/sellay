# Use root/example as user/password credentials
version: '3.1'

services:
  # app:
  #   image: 390402546635.dkr.ecr.eu-north-1.amazonaws.com/sellay:1.0
  #   restart: always
  #   ports:
  #     - 3000:3000
  # NODE_ENV: PROD

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    volumes:
      - ${PWD}/../volumes:/data/db
